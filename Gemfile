source "https://rubygems.org"

# This gem bundles the exact Jekyll + plugin versions GitHub Pages
# uses to build your site, so what you see locally matches what
# GitHub publishes.
gem "github-pages", group: :jekyll_plugins

# Required for local `jekyll serve` on newer Ruby versions.
gem "webrick"
